service: notes-crud-api
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello
